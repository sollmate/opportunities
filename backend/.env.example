# Comma-separated list of allowed CORS origins (the frontend dev server)
CORS_ORIGINS=http://localhost:3000

# Placeholder bearer token accepted by the auth stub. Replace with real auth.
DEV_AUTH_TOKEN=dev-secret-token
